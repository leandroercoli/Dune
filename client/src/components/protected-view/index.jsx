//TODO
export function ProtectedView({ children }) {
  return children;
  //   const { secondaryRole } = useSelector((state) => state.login);
  //   return secondaryRole === ROLE_CLIENT_ADMIN ? children : null;
}
